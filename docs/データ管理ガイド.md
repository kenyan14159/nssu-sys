# 🗄️ データ管理ガイド（初心者向け）

このドキュメントでは、Nit-Sys で登録されたアカウントや選手データがどこに保存され、どのように管理されているかを説明します。

---

## 📍 データはどこに保存されている？

### 開発環境（ローカル）の場合

プロジェクトフォルダ内の **`db.sqlite3`** というファイルにすべてのデータが保存されています。

```
nit-sys/
├── db.sqlite3    ← ★ ここにデータが保存されている！
├── manage.py
├── ...
```

> 💡 **SQLite** とは、ファイル1つで動く軽量なデータベースです。開発・テスト用に便利です。

### 本番環境の場合

本番環境（Render など）では **PostgreSQL** というデータベースサーバーにデータが保存されます。
これは `DATABASE_URL` という環境変数で設定されています。

---

## 👤 どんなデータが保存されている？

### 1. ユーザー（アカウント）情報

| 項目 | 説明 |
|------|------|
| メールアドレス | ログインに使用 |
| パスワード | 暗号化して保存 |
| 氏名・フリガナ | 表示名 |
| 電話番号 | 連絡先 |
| 所属団体 | どの大学・クラブに所属しているか |
| 権限 | 管理者かどうか |

### 2. 団体情報

| 項目 | 説明 |
|------|------|
| 団体名 | 大学名やクラブ名 |
| フリガナ | 並び替え用 |
| 略称 | スタートリストなどで使用 |
| 代表者情報 | 氏名・メール・電話番号 |

### 3. 選手情報

| 項目 | 説明 |
|------|------|
| 氏名・フリガナ | 選手の名前 |
| 生年月日 | 資格確認用 |
| 性別 | 種目振り分け用 |
| 所属団体 | どの団体の選手か |
| 登録番号 | 陸連登録番号など |

---

## 🔍 データを確認する方法

### 方法1: Django 管理画面（Admin）を使う ⭐おすすめ

1. 開発サーバーを起動
   ```bash
   python manage.py runserver
   ```

2. ブラウザで以下にアクセス
   ```
   http://localhost:8000/admin/
   ```

3. 管理者アカウントでログイン

4. 「ユーザー」「団体」「選手」などをクリックしてデータを確認・編集

> 💡 管理者アカウントがない場合は作成してください：
> ```bash
> python manage.py createsuperuser
> ```

### 方法2: Django シェルを使う

ターミナルでデータを直接確認できます。

```bash
# Django シェルを起動
python manage.py shell
```

```python
# ユーザー一覧を確認
from accounts.models import User
User.objects.all()

# 選手一覧を確認
from accounts.models import Athlete
Athlete.objects.all()

# 団体一覧を確認
from accounts.models import Organization
Organization.objects.all()

# 終了
exit()
```

### 方法3: データベースファイルを直接見る

SQLite のデータベースファイルを直接開くこともできます。

**VS Code の場合:**
1. 拡張機能「SQLite Viewer」または「SQLite」をインストール
2. `db.sqlite3` ファイルをクリック
3. テーブル一覧が表示される

**コマンドラインの場合:**
```bash
# SQLite を起動
sqlite3 db.sqlite3

# テーブル一覧を表示
.tables

# ユーザーテーブルの中身を見る
SELECT * FROM accounts_user;

# 終了
.exit
```

---

## 📊 テーブル構成（データベースの中身）

```
┌─────────────────────────────────────────────────────────────┐
│                     データベース (db.sqlite3)                 │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────┐    ┌──────────────────┐               │
│  │  accounts_user   │    │ accounts_athlete │               │
│  │  (ユーザー)       │    │  (選手)          │               │
│  │                  │    │                  │               │
│  │ - email          │    │ - name           │               │
│  │ - password       │    │ - name_kana      │               │
│  │ - full_name      │    │ - birth_date     │               │
│  │ - organization   │───→│ - organization   │               │
│  └──────────────────┘    └──────────────────┘               │
│           │                       │                          │
│           │                       │                          │
│           ▼                       ▼                          │
│  ┌──────────────────┐    ┌──────────────────┐               │
│  │accounts_organiza │    │  entries_entry   │               │
│  │    tion (団体)    │    │  (エントリー)     │               │
│  │                  │    │                  │               │
│  │ - name           │    │ - athlete        │               │
│  │ - short_name     │    │ - race           │               │
│  │ - representative │    │ - entry_time     │               │
│  └──────────────────┘    └──────────────────┘               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚠️ 注意事項

### データのバックアップ

開発環境のデータをバックアップしたい場合：

```bash
# db.sqlite3 をコピーするだけ！
cp db.sqlite3 db.sqlite3.backup
```

### データの初期化（リセット）

すべてのデータを消して最初からやり直したい場合：

```bash
# 1. データベースファイルを削除
rm db.sqlite3

# 2. マイグレーションを再実行
python manage.py migrate

# 3. 管理者を作成
python manage.py createsuperuser
```

### 本番環境のデータには注意！

本番環境（Render など）のデータを操作する場合は、**必ずバックアップを取ってから** 行ってください。

---

## 🆘 よくある質問

### Q: 管理画面にログインできない

**A:** 管理者アカウントを作成してください：
```bash
python manage.py createsuperuser
```

### Q: データベースのエラーが出る

**A:** マイグレーションを実行してください：
```bash
python manage.py migrate
```

### Q: 開発用のテストデータを入れたい

**A:** Django シェルでデータを作成できます：
```python
python manage.py shell

from accounts.models import Organization, User

# 団体を作成
org = Organization.objects.create(
    name='テスト大学',
    name_kana='テストダイガク',
    representative_name='山田太郎',
    representative_email='test@example.com',
    representative_phone='03-1234-5678'
)

# ユーザーを作成
user = User.objects.create_user(
    email='user@example.com',
    password='password123',
    full_name='田中花子',
    full_name_kana='タナカハナコ',
    phone='090-1234-5678',
    organization=org
)
```

---

## 📚 関連ファイル

| ファイル | 説明 |
|----------|------|
| `accounts/models.py` | ユーザー・団体・選手のデータ構造を定義 |
| `nitsys/settings.py` | データベース接続設定 |
| `db.sqlite3` | 開発用データベースファイル |
| `accounts/admin.py` | 管理画面の設定 |

---

*最終更新: 2025年11月*
